interface User {
    id: string,
    name: string,
    email: string,
    shopkeeper: boolean,
    document: number,
    address: String,
    addresNumber: number,
    phone: number
}

export default User;